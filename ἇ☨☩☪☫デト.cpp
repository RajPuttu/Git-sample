.Clone from github -- add AR stream as second remote and perform push/pull operations

Yes	
Isaac: clone from git, and push to AccuRev; also test the sub-directory (e.g. accurev://host:port/stream/dir1/dir2) and make sure contents outside of the directory will not get pushed to AccuRev

2.Clone from AR -- add git as second remote and perform push/pull operations	Yes	
Isaac: same as above
